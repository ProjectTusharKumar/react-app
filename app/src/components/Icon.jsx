import React from 'react';

const Icon = ({ name, color, size = 24 }) => {
  // Simple icon placeholder, replace with real icons as needed
  return <span style={{ fontSize: size, color, marginRight: 8 }}>{name}</span>;
};

export default Icon;
